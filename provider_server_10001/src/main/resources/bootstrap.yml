

spring:
  application:
    name: provider-server
  profiles:
    active: 10001
  cloud:
    config:
      discovery:
        service-id: server-config
        enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://10.12.159.30:8080/eureka
